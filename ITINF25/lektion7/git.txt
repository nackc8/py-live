Nytt repo:

git init

Status via:

git status

Lägg till en fil:

    Enskilda filer: git add filnamn1 filnamn2 ...

    Alla filer: git add .

Commita med:

git commit

Se alla commits:

git log

Se vad jag ändrat

git status -- för filer
git diff -- se ändringar

OJ! 
